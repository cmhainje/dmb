version: '3.8'
services:
  dev:
    build: .
    volumes:
      - .:/workspace
      - dev-cache:/root/.cache
    working_dir: /workspace
    stdin_open: true
    tty: true
    cpus: 5

volumes:
  dev-cache:
